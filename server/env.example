# ============================================
# SERVER CONFIGURATION
# ============================================
# Port number on which the server will run
# Default: 3000
PORT=3000

# ============================================
# DATABASE CONFIGURATION
# ============================================
# MongoDB connection string
# Format: mongodb://[username:password@]host[:port][/database]
# Example: mongodb://localhost:27017/inexmanager
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/inexmanager
MONGODB_URI=mongodb://localhost:27017/inexmanager

# ============================================
# AUTHENTICATION & SECURITY
# ============================================
# Secret key used to sign and verify JWT access tokens
# IMPORTANT: Use a strong, random string in production
# Generate using: openssl rand -base64 32
# This token is used for regular authentication tokens (expires in 7 days by default)
JWT_SECRET=your-secret-key-change-this-in-production

# Secret key used to sign and verify JWT refresh tokens
# IMPORTANT: Use a different strong, random string from JWT_SECRET
# Generate using: openssl rand -base64 32
# This token is used for refreshing access tokens (expires in 30 days by default)
JWT_REFRESH_SECRET=your-refresh-secret-key-change-this-in-production

# JWT access token expiration time
# Format: number followed by unit (s=seconds, m=minutes, h=hours, d=days)
# Examples: 15m, 1h, 7d, 3600s
# Default: 15m (15 minutes)
JWT_ACCESS_EXPIRES_IN=15m

# JWT refresh token expiration time
# Format: number followed by unit (s=seconds, m=minutes, h=hours, d=days)
# Examples: 7d, 30d, 14d
# Default: 7d (7 days)
JWT_REFRESH_EXPIRES_IN=7d

# ============================================
# CLIENT CONFIGURATION
# ============================================
# Frontend application URL
# Used for generating email verification and password reset links
# Example: http://localhost:8081 (development)
# Example: https://yourdomain.com (production)
CLIENT_URL=http://localhost:8081

# ============================================
# EMAIL CONFIGURATION (SMTP)
# ============================================
# SMTP server hostname
# For Gmail: smtp.gmail.com
# For Outlook: smtp-mail.outlook.com
# For custom SMTP: your-smtp-server.com
EMAIL_HOST=smtp.gmail.com

# SMTP server port
# Common ports: 587 (TLS), 465 (SSL), 25 (unencrypted)
# Gmail uses: 587
EMAIL_PORT=587

# SMTP authentication username (usually your email address)
# For Gmail: your-email@gmail.com
# Note: For Gmail, you need to use an "App Password" instead of your regular password
EMAIL_USER=your-email@gmail.com

# SMTP authentication password
# For Gmail: Generate an "App Password" from Google Account settings
# For other providers: Use your email account password or app-specific password
# Gmail App Password guide: https://support.google.com/accounts/answer/185833
EMAIL_PASS=your-app-password

# Email address that will appear as the sender
# This is the "From" field in emails sent by the application
# Example: noreply@inexmanager.com
# Note: Some SMTP providers require this to match EMAIL_USER
EMAIL_FROM=noreply@inexmanager.com

# ============================================
# EMAIL TEMPLATE CONFIGURATION (Optional)
# ============================================
# Support email address displayed in email templates
# Default: support@inexmanager.com
SUPPORT_EMAIL=support@inexmanager.com

# Company address displayed in email footer
# Default: 123 Finance Street, Money City, FC 12345
COMPANY_ADDRESS=123 Finance Street, Money City, FC 12345

# Social media links for email templates (optional)
# Leave as '#' if not using social media
FACEBOOK_LINK=#
TWITTER_LINK=#
INSTAGRAM_LINK=#
